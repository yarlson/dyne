#!/usr/bin/env bash
set -euo pipefail

case "${1:-}" in
  *Username*)
    printf '%s\n' x-access-token
    ;;
  *Password*)
    cat /var/run/git-auth/token
    printf '\n'
    ;;
  *)
    exit 1
    ;;
esac
